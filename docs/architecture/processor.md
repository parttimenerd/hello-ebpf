# Annotation Processor

!!! note "Placeholder"
    This page is a placeholder for content coming in Spec 2. See the
    [plugin audit](../superpowers/audit/audit-plugin.md)
    for the extension points it will cover.

Planned scope:

- The `bpf-processor` module: what it does vs. the compiler plugin
- `@BPFMapClass` and `@BPFMapDefinition` processing
- `TypeProcessor` and struct layout resolution
- BTF map definitions emitted to `.bpf.c`
