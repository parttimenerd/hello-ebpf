# Install & Prerequisites

!!! note "Placeholder"
    This page is a placeholder for content coming in Spec 2. See the
    [runtime API audit](../superpowers/audit/audit-runtime-api.md)
    for the symbols it will cover.

Planned scope:

- Prerequisites: kernel ≥ 6.6, clang, libbpf, bpftool
- Using the Maven archetype (`bpf-archetype`) as the one-command scaffold
- Manual dependency setup via pom.xml
- Verifying the installation with `HelloWorld.java`
