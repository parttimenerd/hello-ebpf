# Your First BPF Program

!!! note "Placeholder"
    This page is a placeholder for content coming in Spec 2. See the
    [samples audit](../superpowers/audit/audit-samples.md)
    for the symbols it will cover.

Planned scope:

- The `HelloWorld.java` sample end-to-end walkthrough
- `@BPF`, `@BPFFunction`, `BPFProgram.load()`
- Running with `sudo`
- Reading `bpf_trace_printk` output from the trace pipe
